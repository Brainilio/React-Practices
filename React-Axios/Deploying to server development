Routing & the server (deployment)

User => Server(Handles Requests first!)
(If route is unknown to the server; you'll get a 404)

You have to configure server in a way that itll always 
sends forward a request, so always show it to the client 


always load index.html
          v 
          v
          v
          v
          v

React App (Knows the Routes)! 
Always set a base path! in react router
